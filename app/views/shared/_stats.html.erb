<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>", id= "following">
    <strong id="following" class="stat">
      <%= @user.following.count %>
    </strong>
    フォロー
  </a>
  <a href="<%= followers_user_path(@user) %>", id= "follower" >
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    フォロワー
  </a>
</div>